# Layout flow — organic transition from homepage

Status: resolved
Type: grilling
Blocked by: 01

## Answer

Mirrored `ModeTiles`: `grid md:grid-cols-3` sticker cards identical in structure to `src/components/home/ModeTiles.tsx`, selected card stuck-pressed, `OptionHeader` moves inside the expanded tile, settings panel renders directly underneath as one expanded sticker. Home card tap → same card enlarged.

## Comments

> *This was generated by AI during triage.*
> Tapped card id must drive `persistence.progress.load("mode")` preselect — see `bugs/02-mode-preselect` for the current bug where it does not.

## Question

What layout makes the content "flow very organically" from the homepage's five-section rhythm (hero → marquee → three tiles → grown-ups → footer) into this screen's `ModeTabs` + `OptionHeader` + panel content?

Options that keep the way open: (a) reuse `PhotoMarquee`'s horizontal strip as a top accent, (b) turn Mode Cards into large sticker tiles like `Home/ModeTiles.tsx`, (c) keep tabs but restyle them to the homepage's border-4 sticker language. Which literal device makes the transition read as one app, not two?
