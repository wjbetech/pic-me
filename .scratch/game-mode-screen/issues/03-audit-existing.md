# Audit existing Game Options implementation before redesign

Status: ready-for-agent
Type: task

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Inventory the current Game Options screen so the redesign can preserve functionality byte-for-byte

**Current behavior:**
The screen lives at `src/components/GameOptions/` with `GameOptions.tsx` as the container. It renders `ModeTabs` (three mode headings), `OptionHeader` description, and mode-specific panels (`MultipleChoiceSettings`, `HangmanSettings`, `OpenAnswerSettings`). Selection and settings persist via the session-persistence module and must survive refresh. The page is currently a centered white card — the redesign must replace this with sticker-book language without changing what it does.

**Desired behavior:**
A written inventory (posted as a comment on this ticket, not a code change) that lists every interactive element that must remain: mode switch tabs, `OptionHeader` copy slot, hint toggles (`mcHints`/`hangmanHints`), rounds selector, blur slider, `showDescription` toggle, lives stepper, and any persistence keys. The report should also note the `CONTEXT.md` terms that apply (Game Mode, Mode Card, Game Options screen).

**Key interfaces:**
- `GameOptions` props and its persistence contract — what it currently reads/writes vs what it must continue to read/write after the redesign
- Each settings panel's props — what types and ranges they expose

**Acceptance criteria:**
- [ ] Comment lists every interactive control with its current type/range and persistence key
- [ ] Comment flags which controls are per-mode vs shared
- [ ] Comment confirms `CONTEXT.md` glossary terms were used

**Out of scope:**
- Any visual redesign or code change — this ticket is inventory only
- New settings fields (see `game-settings-v2` map)
