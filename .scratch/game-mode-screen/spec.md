# Spec — Game Mode Screen Redesign

Covers inbox: "Redesign this page to thematically match the home page more. ... Ensure very strong contrast ... Use design skills + grill-me and wayfinder ... The options/current functionality are great, just redesign the page to match the app."

## Current

`src/components/GameOptions/GameOptions.tsx` — white `bg-base-100` card, `ModeTabs` (three headings), `OptionHeader` description, mode-specific settings panels, persisted selection.

## Desired

Same functionality, new skin that reads as the same design system as the new homepage: Fredoka/Nunito, sticker-book playful, photo-showcase accents where appropriate, strong contrast in both themes. Organic flow from homepage's rhythm.

Child tickets `issues/01-*` (visual language) and `02-*` (layout flow) are the decisions; `03-*` is the audit that unblocks them.
