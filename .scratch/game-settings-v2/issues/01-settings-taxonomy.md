# What settings does each mode actually need?

Status: resolved
Type: grilling

## Answer

Taxonomy ships minimal: **rounds + difficulty (Easy/Medium/Hard/All pool filter via `Animal.difficulty`)** for every mode, both chosen **pre-game** on the Game Options screen (persists via `persistence`, read on entry — no mid-game ramping in v2). Time pressure, hint budgets, and image-obscuring beyond `blur` graduate later as separate tickets once rounds+difficulty are playtested.

## Comments

> *This was generated by AI during triage.*
> Owner confirmed 23/08: "easy/medium/hard/all is good" + "Go with your recommendation for now" (stop at two settings) + "pre-game" (no mid-game progression).

## Question

Draft the full settings taxonomy per mode — starting from your inbox seed (number of rounds, animal difficulty, etc.) plus what's already there (blur, showDescription, hint type, lives, rounds).

For each proposed setting, answer: which mode(s) does it belong to, what type/range does it have, and what user flow exposes it (pre-game dropdown vs in-game toggle)? The "etc." is the core of the grilling — we'll fan out breadth-first before ticketing.

We must not leak another mode's concepts (e.g., Hangman's lives slider shouldn't appear on Multiple Choice).
